if [ -z "$2" ]; then
  A=$(curl -T "$1" https://pixeldrain.com/api/file/)
elif [ "$2" == "in" ]; then
  read -s I
  A=$(curl -T "$1" -u :$I https://pixeldrain.com/api/file/)
else
  A=$(curl -T "$1" -u :$2 https://pixeldrain.com/api/file/)
fi
AA=$(echo $A | cut -d'"' -f4)
echo -e "\n    https://pixeldrain.com/u/$AA\n"

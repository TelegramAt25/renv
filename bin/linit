#!/bin/bash

LMDIR=.repo/local_manifests
[ -z "$1" ] && { echo "please specify a branch" && exit 1 ; }

if [ -d "$LMDIR" ]; then
    rm -rf "$LMDIR"
fi

git clone -q $2 $LMDIR -b $1
rm -rf "$LMDIR/.git"
